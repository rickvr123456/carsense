import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF2BE079);
  static const Color background = Color(0xFF0F1418);
  static const Color surface = Color(0xFF1E2A35);
  static const Color cardBackground = Color(0xFF222b35);
  static const Color inputBackground = Color(0xFF1B222A);

  static const Color success = Color(0xFF2BE079);
  static const Color error = Colors.red;
  static const Color warning = Colors.orange;
  static const Color info = Color(0xFF3660EF);

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textOnPrimary = Colors.black;

  static const Color connected = Colors.greenAccent;
  static const Color disconnected = Colors.redAccent;
}
